# GhostPilot Requirements
# NOTE: ROS2 packages (rclpy, geometry_msgs, nav2_msgs, etc.) are NOT pip-installable.
# Install ROS2 Humble first: https://docs.ros.org/en/humble/Installation.html
# Then: sudo apt install ros-humble-navigation2 ros-humble-nav2-bringup

# Pure Python dependencies (pip-installable)
numpy>=1.24.0
opencv-python>=4.8.0
pyyaml>=6.0
requests>=2.31.0
scipy>=1.10.0

# Optional: Local LLM for agentic layer
# ollama  # Install separately: curl -fsSL https://ollama.com/install.sh | sh